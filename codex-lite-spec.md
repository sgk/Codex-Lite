# Codex Lite 仕様書

## 1. 概要

Codex Lite は、Windows の単一ウィンドウから、WSL 上の複数プロジェクトと Codex チャットを扱う軽量クライアントである。Windows 側は表示と操作を担う WPF アプリ、WSL 側は状態管理と Codex 連携を担う Python デーモンとする。

Codex の推論やツール実行は再実装せず、Codex CLI の `app-server` を利用する。Codex アプリの既存チャットについては、ローカルの state SQLite から必要最小限のメタデータを読み取り、公開されたJSONL履歴ファイルから表示内容を構築する。

本書は将来構想ではなく、現在の実装を記述する。

## 2. 構成

```text
Windows
  CodexLite.exe / CodexLite.dll（C#、.NET 8、WPF）
    │ wsl.exeで起動
    │ HTTP/JSON + SSE（127.0.0.1、動的ポート）
    ▼
WSL
  run-daemon.sh
    └─ Python仮想環境
        └─ Starletteデーモン
            ├─ Codex Lite SQLite
            ├─ Codex state SQLite（読み取り専用）
            ├─ Codex JSONL履歴（読み取り専用）
            └─ codex app-server（標準入出力JSON-RPC）
```

Windowsアプリはビルド成果物に同梱された `run-daemon.sh` と `daemon/` だけを使用し、開発用チェックアウトなど別経路へのフォールバックは行わない。

## 3. 対象環境

- Windows 11、WSL 2
- WSLディストリビューションはWindows側で設定された既定環境を起動時に自動検出する
- Windows側: .NET 8 WPF
- WSL側: Python 3.11以上、Starlette、uvicorn、SQLite
- WSLから実行可能で、ユーザーによる認証が完了した Codex CLI

プロジェクトは実在する絶対パスに限る。Linuxパスに加え、設定で許可されている場合は `/mnt/c` 上のパスも扱う。

## 4. Windowsクライアント

### 4.1 画面構成

左ペインにプロジェクトとチャットのツリー、右ペインに選択対象の内容を表示する。右ペインには次のタブがある。

- 「会話」: ユーザー指示、Codexの作業中メッセージ、ツール実行状況、最終回答を時系列表示する。プロジェクトツリーでチャットを選択してから最新の履歴を表示できるまで、会話欄に対象チャット名と経過時間を含む進捗表示を重ねて読み込み中であることを明示する。この履歴読み込みではウィンドウ右下の共通進捗バーを併用せず、同じ処理を二か所に重複表示しない。右下の共通進捗バーは、チャット実行やファイル処理などのアプリ全体またはバックグラウンドの処理に使用する。
- 「ファイル」: プロジェクト配下のディレクトリツリーとファイル内容を読み取り専用で表示する。Markdownは専用表示を使い、リンクからプロジェクト内の別ファイルへ移動できる。基本的なMarkdown表は罫線付きの表として表示する。VS Codeまたは既定アプリで開く操作も提供する。選択したディレクトリはWindows Explorerでその場所を開き、選択したファイルは親ディレクトリを開いて当該ファイルを選択状態にできる。ウィンドウ右下のExplorer操作は従来どおりプロジェクトルートを開く。ファイルツリーで選択したファイルまたはディレクトリはWindows Explorerへコピーでき、Explorerでコピーした項目は選択中のプロジェクト内ディレクトリへ貼り付けられる。貼り付け時に同名項目が存在する場合は上書きしない。ファイルツリーの右クリックメニューでは、選択項目の名前またはプロジェクト内のWSL絶対パスをテキストとしてコピーできる。
- 「オートメーション」: 選択チャットに紐づく定期実行を作成、編集、有効化、即時実行、削除する。
- 「診断」: app-server、実行中Run、Codex、権限設定、デーモン診断情報を表示する。

下部には現在の状態、Explorer・VS Codeでプロジェクトを開くボタン、設定ボタンを置く。

### 4.2 プロジェクトとチャット

プロジェクトはディレクトリ指定で登録するほか、Codex履歴から候補を検出して一括登録できる。表示名の変更、登録解除、ドラッグによる並べ替えに対応する。登録解除はCodex側のプロジェクトやファイルを削除しない。

表示名の変更とは別に、プロジェクトツリーから実際のプロジェクトディレクトリの親ディレクトリとディレクトリ名を変更できる。移動先は未作成でなければならず、同一ファイルシステム内の原子的な移動だけを行う。filesystem root、ユーザーhome、プロジェクト自身の配下、別ファイルシステム、既存の移動先、実行中のチャットまたはオートメーションがある場合は拒否し、コピーや上書きへのフォールバックは行わない。実ディレクトリの移動後に `projects.path` を更新し、DB更新に失敗した場合はディレクトリを元の場所へ戻す。表示名、チャット、provider側thread ID、履歴は維持し、以後の実行では新しいパスをcwdとして使う。Codex private DBは変更しない。

チャットはプロジェクト配下へ表示し、選択、絞り込み、名前変更、アーカイブ、ドラッグによる並べ替えができる。並べ替え順はWindowsクライアントのUI状態として保存する。プロジェクトだけを選ぶと新規チャット入力欄を表示し、最初の送信時にスレッドを作成する。新設したチャットは、閉じていたプロジェクトを展開したうえで選択状態にする。既存チャットを選ぶと履歴と入力欄を表示する。

履歴は最新30件を先に読む。初回だけ履歴一覧を非表示のまま組み立て、最後の実メッセージの画面要素を生成し、レイアウトと末尾位置を2回確認してから表示する。組み立て途中のスクロール位置は画面に出さない。未ロード範囲は件数に比例する巨大な空白ではなく固定高さの案内として上端に表示し、利用者がホイール、Home/PageUp、スクロールバーを操作してそこへ近づいたときだけ過去分を追加する。レイアウト変更やプログラムによるスクロール通知だけでは過去分を自動ロードせず、未ロード案内の高さとロード状態が変わらない場合は先頭項目を置換しない。Windowsクライアントの起動中は、チャットごとに一度読み込んだ履歴本文、追加済みの過去範囲、総件数、未読込範囲に加え、履歴一覧、仮想表示、生成済みMarkdown、スクロール位置を含む画面部品を非表示状態で保持する。チャットを再選択したときは同じ画面部品を再表示し、表示コレクションの差し替え、スクロール位置の再適用、JSONL履歴の再読込を行わない。スクロール位置はアプリ終了をまたいで保存しない。会話一覧はピクセル単位で仮想化し、長い履歴でも表示範囲とその前後だけメッセージ要素およびMarkdown文書を生成する。予約済みの自動末尾移動は、その後に利用者がスクロールまたはキー操作を行った場合は適用しない。末尾追従中は応答本文の伸長やMarkdown再レイアウトによって一時的に末尾から離れても追従を維持し、ホイール、Home/PageUp/PageDown、スクロールバーなど利用者自身のスクロール操作後にだけ追従状態を更新する。定期更新と実行完了後の再読込結果は保持内容へ反映する。Runの成功通知を受けたときは、選択中かどうかにかかわらずdaemonからそのRunの保存済み結論を取得し、チャット別の保持内容にある仮の待機・結論表示を正式な結論へ置き換える。保存済み結論を取得できない場合は、完了したように黙って停止せず、その旨を結論欄に明示する。CODEX_HOMEまたはdaemonの再起動時と、対象チャットまたはプロジェクトを一覧から除いたときは、対応する画面部品を破棄する。外部で履歴が更新されても、利用者が末尾付近を見ていない限りスクロール位置を末尾へ強制移動しない。

### 4.3 メッセージ送信

Enterで送信し、改行操作は修飾キーを使う。送信履歴は上下キーで再利用できる。ファイル選択、ドラッグ＆ドロップ、クリップボード画像から添付を追加できる。添付パスはWSLパスへ変換してapp-serverへ渡す。非画像ファイルは、Codexが同名ファイルを別の場所から探索せず、変換後の絶対パスを直接確認するよう送信内容にも明示する。

送信後はSSEでイベントを受け取り、作業中の説明、ツール実行、回答の差分、完了・失敗を逐次反映する。チャット本文およびMarkdownファイルのプレビューでは、空行で区切られた通常段落の後に1行分の余白を表示し、先頭の空白またはタブに応じて箇条書きと番号付き箇条書きを階層表示し、折り返し行を項目本文の開始位置へ揃える。通常テキストでは `&nbsp;`、`&lt;`、`&#160;` などのHTML文字参照を表示文字へ変換するが、フェンス付きコードブロック、インラインコード、リンク先URL、画像パスでは変換しない。`~~文字列~~` を取り消し線として表示し、`![代替テキスト](画像パス)` を画像として埋め込む。フェンス付きコードブロックにはコード本文だけをクリップボードへ保存する、背景と枠線のないコピーアイコンを表示する。チャット内の相対画像パスはプロジェクトルート、Markdownファイル内ではそのファイルの親ディレクトリを基準にする。画像はproject root外やシンボリックリンクを読まないdaemonの読み取り専用APIを通して取得し、読み込み失敗時と外部URLでは代替テキストをクリック可能なリンクとして表示する。会話本文とMarkdownプレビューの画像は表示要素の生成時に一度だけ取得し、その表示要素が残る間は元ファイルが変更されても画像を差し替えない。アプリ全体で画像パス単位の共有キャッシュは行わず、表示要素を新たに生成したときはその時点のファイル内容を取得する。外部画像URLを画面表示だけで自動取得しない。実行中は停止できる。また、同じチャットの実行中に入力した指示は `turn/steer` により追加指示として送る。画面上は実行中でもapp-server側ですでにターンが完了していた場合、入力内容を失敗させず同じスレッドの次ターンとして開始する。停止はapp-serverの `turn/interrupt` を応答待ちのリクエストとして送り、スレッド状態が `idle` になるまで追加指示と次ターン開始を無効にし、停止前後のターンIDを混在させない。app-serverが終了した場合は待機中のRunを失敗として終了し、完了通知を取り逃した場合も定期的なスレッド状態確認で `idle` へ収束させ、Runを無期限に実行中のまま残さない。`thread/read` による状態照合が一時的に失敗してもapp-serverのターンを停止せず、Run監視を継続して照合失敗を診断へ記録する。

プロジェクトツリーでは実行中のチャット行にスピナーを表示する。配下に実行中チャットがあるプロジェクトを折りたたんでいる間は、見えないチャット行の代わりにプロジェクト行へスピナーを表示し、展開中はプロジェクト行へ重複表示しない。

会話、ファイル、オートメーション、診断のタブ選択はチャットセッションごとにUI状態へ保存し、チャットを切り替えたときとアプリ起動時に対象チャットの前回選択へ戻す。プロジェクトだけを選択している画面のタブ選択は、チャット別の選択とは分けて保存する。

チャット本文またはMarkdownプレビュー内のプロジェクト内ファイルリンクをクリックしたときは、ファイルタブへ切り替え、途中のディレクトリを展開して対象ファイルを選択する。リンク末尾の `:行番号` または `:行番号:列番号` はファイル名に含めず、表示位置情報として取り除いてファイルを解決する。タブ切替とディレクトリ展開後はツリー項目のUI生成を待ってから次の項目を選択し、タブだけが開いて対象ファイルが未選択になる競合を避ける。ファイルタブの画像プレビューはURIキャッシュを使わず、ファイル選択時にdaemonの読み取り専用APIから最新のバイト列を取得する。Markdown本文の文字要素上でマウス操作が始まった場合も、文章要素と画面要素の両方の親階層を安全にたどり、スクロールバー判定などの入力処理で例外を出さない。

プレーンテキストおよびMarkdownファイルのプレビューでは、マウスホイールの入力量を縮小せず、通常のホイール操作で十分な距離を縦スクロールする。チャット内のMarkdown表示は、会話一覧全体をスクロールする既存動作を維持する。

同時実行はチャットをまたいで管理し、既定の上限は4である。

クリップボード画像などの一時添付ファイルは `%LOCALAPPDATA%\CodexLite\attachments` に保存する。Windowsクライアント起動時に、7日より古い一時添付ファイルを削除する。削除に失敗したファイルがあっても起動は継続する。

### 4.4 使用状況

プロジェクト選択時の新規チャット画面に、Codexから取得した5時間容量と1週間容量を数値とグラフで表示する。利用者は手動で再読み込みできる。使用状況を開いたときの自動更新と手動の再読み込みでは、モデル一覧も同時に更新する。この機能はapp-serverモードでのみ利用できる。

### 4.5 設定

次のUI設定をWindows側に保存し、次回起動時に復元する。

- ウィンドウ位置とサイズ
- 左ペイン幅、展開中のプロジェクト、選択状態、プロジェクト順
- 文字サイズ、ファイル表示の折り返し
- Codex HOMEの選択

Codex HOMEは「自動」「Windows側 `.codex`」「WSL側 `$HOME/.codex`」から選ぶ。自動ではWindowsユーザープロファイルの `.codex` が存在すればそれを優先する。変更時はデーモンを再起動し、プロジェクトツリーを読み直す。複数のHOMEを同時に統合しない。

Codexの実行設定はWSL側の `settings.json` に保存し、スレッド作成時と送信前にapp-serverへ適用する。Windowsクライアント起動時はプロジェクトツリーの選択状態を復元する前にアプリ全体の設定を読み込み、チャット選択時と起動時のチャット選択復元時にチャット固有の保存設定で上書きする。承認レベル、モデル、思考レベルの各メニューは、表示対象の保存設定を読み込み終えるまで無効化し、チャット入力欄の編集は妨げない。

モデルと承認レベルはチャット入力欄から選択する。モデル候補はデーモンの `/models` を介して app-server の `model/list` から取得し、新しいモデルIDが返ればクライアントの変更なしで表示する。「既定」は空のモデル値としてCodex側の既定に従う。app-serverでモデル一覧を取得できない場合に限り、デーモンが持つ最小限の静的候補を表示する。DeepSeek APIキーがWSL側のユーザー専用ファイル（`~/.config/codex-lite/deepseek.env`）に設定されている場合は `deepseek-v4-flash` と `deepseek-v4-pro` と画像入力対応の `deepseek-v4-flash-vision-exp` を候補へ追加し、同じAPIキー、接続先、DeepSeek用 `CODEX_HOME` を共有してチャットごとに切り替える。

OpenAIとDeepSeekの切り替えは、チャットごとのモデル選択に応じて、それぞれ専用のCodex app-serverと内部スレッドを使い分ける。各provider app-serverは必要になった時だけ起動し、実行中Runがなく最後の利用から60秒経過したら個別に停止する。DeepSeekを選択した場合はDeepSeekのResponses API設定とモデルカタログを使う。DeepSeek用の `CODEX_HOME` を準備するとき、OpenAI用の `CODEX_HOME` に存在する `AGENTS.md` と `AGENTS.override.md` へのシンボリックリンクを作成し、既存のDeepSeek側ファイルは上書きしない。元のOpenAI JSONLを持つチャットは、DeepSeek用の内部スレッドが追加された後も元のOpenAI thread IDを再利用し、別のOpenAIスレッドを作らない。プロバイダーを切り替えたときは、切替先を最後に使用してから増えたユーザー指示と最終回答だけを表示会話の文脈として渡し、切替先自身が保持する履歴、提供元固有のreasoning、作業中表示、ツール詳細、過去に合成した引き継ぎ文脈を重複して渡さない。内部引き継ぎ文から始まるスレッドはユーザー向けチャット一覧へ取り込まない。切替先を初めて使う場合も、引き継ぎ文脈はメッセージ境界を保って上限内に収める。実行中のRunを切断する切り替えは行わず、APIキーの内容はログ・診断・画面へ出さない。

自動圧縮は app-server 起動時の Codex config として有効化する。既定では
`model_auto_compact_token_limit=100000` と
`model_auto_compact_token_limit_scope="body_after_prefix"` を指定し、圧縮時に引き継いだprefixを次回の自動圧縮閾値へ重ねて算入せず、圧縮後に増えた本文を基準とする。同じ app-server 上で継続・新規作成される全チャットセッションに適用する。`CODEX_LITE_AUTO_COMPACT_TOKEN_LIMIT=0` を指定した場合は Codex Lite から自動圧縮設定を渡さない。

承認モードはCodexデスクトップアプリに合わせ、チャット入力欄から次の3つを選択する。

- 承認を求める: `permissions=:workspace`、`approvalPolicy=on-request`、`approvalsReviewer=user`
- 自動で承認: `permissions=:workspace`、`approvalPolicy=on-request`、`approvalsReviewer=auto_review`
- フルアクセス: `permissions=:danger-full-access`、`approvalPolicy=never`、`approvalsReviewer=user`

承認モードはサンドボックス境界と承認要求の扱いを一体として切り替える。別のアクセスポリシーを診断画面から変更する経路は設けない。思考レベルはモデル一覧が返す `supportedReasoningEfforts` を使い、モデルごとの候補をチャット入力欄に表示する。候補を取得できない場合は「既定」「低」「中」「高」「最大」を表示し、選択値はapp-serverの `thread/settings/update` の `effort` に渡す。

app-serverから `item/commandExecution/requestApproval` または `item/fileChange/requestApproval` のサーバー要求を受けた場合は、通常のクライアント要求への応答と区別してチャットのRunへ関連付ける。Windowsクライアントは承認内容をダイアログに表示し、承認・拒否・キャンセルの結果を元のJSON-RPC request IDへ返す。未対応のサーバー要求を黙って破棄してRunを待機させない。

## 5. デーモン

### 5.1 起動と終了

Windowsアプリは `wsl.exe -d <ディストリビューション> -- bash -c ...` で同梱スクリプトを起動する。起動時に login shell は使わない。これは `.bash_profile` 等で ssh-agent や ssh-add がパスフレーズ入力待ちになり、デーモン起動が止まることを避けるためである。スクリプトは `$HOME/.local/share/codex-lite/daemon-venv` を作成または再利用し、同梱 `pyproject.toml` のハッシュが変わった場合だけパッケージを再導入する。

開発ビルドでは `windows/CodexLite/bin` をビルド成果物、`runtime/CodexLite` を実行用配置先として分離する。ビルドが成功するまでは実行中クライアントを停止しない。成功後の配置は独立したWindowsプロセスへ引き渡し、隣接ディレクトリへのコピー完了後にクライアントを正常終了して実行用ディレクトリを入れ替え、最新クライアントを起動する。デスクトップショートカットは実行用配置先を参照する。

WindowsのRemote同期対応ビルドと配布ZIPには、Firebase SDKを含むRemote Agentを単一のNode用JavaScriptへバンドルし、Firebase公開設定およびAgent既定値とともに `remote-agent/` へ同梱する。OAuthクライアントIDとクライアントシークレットはリポジトリ直下のGit管理外 `.env` だけで管理し、ビルド時に一時設定へ変換してパッケージへ同梱する。`.env` 自体は同梱しない。`GOOGLE_OAUTH_CLIENT_SECRET` が無い場合、Remote AgentとOAuth設定を同梱せず、WindowsアプリのRemote同期UIおよび自動接続を無効にしたビルドを生成する。Windowsアプリは `AppContext.BaseDirectory/remote-agent/index.js` だけをWSLのNode.jsで起動し、開発リポジトリ、ソース版 `remote/`、`node_modules`を探索しない。Firebase同期先は同梱 `firebase-config.json` の `projectId`、`apiKey`、`appId`で決定する。OAuthクライアント設定はGoogleログインにだけ使い、同期先の決定には使わない。

Remote Agentの端末IDはビルドへ固定せず、各PCの初回起動時にランダムIDとPC表示名をWSL ext4側 `~/.local/share/codex-lite/remote-device.json` へ所有者限定で保存する。以後はこのファイルだけを正本とし、ホスト名や同梱設定へフォールバックしない。ファイルが存在するのに不正な場合は新しいIDで置き換えず、同期を停止してエラーを表示する。

初回起動時間と Python バージョン差の影響を抑えるため、デーモンのHTTP層は Starlette と標準 uvicorn を使い、FastAPI/Pydantic や `uvicorn[standard]` のようなネイティブ拡張を含みやすい依存には頼らない。配布ZIPには仮想環境そのものを同梱しない。仮想環境は起動先WSLのPythonで作成し、Python 3.11以上の複数バージョンに対応する。

デーモンは必ず `127.0.0.1` にbindし、既定ではOSが選んだ空きポートを使う。待受完了時、標準出力へ `{"event":"ready","host":"127.0.0.1","port":...}` を1行出す。Windowsアプリはこの行を読んでHTTP接続先を決める。標準エラーはログとして回収する。

Windowsアプリは定期的にhealth checkを行い、デーモンが応答しなければ再起動する。アプリ終了時は最初の終了操作でウィンドウを非表示にし、バックグラウンドで `/shutdown` を呼び、標準入力を閉じる。デーモンは標準入力EOFも終了要求として扱い、app-serverとデータベースを閉じる。

### 5.2 app-server

通常モードは `codex app-server --listen stdio://` を子プロセスとして起動し、改行区切りJSON-RPCで通信する。app-serverは一覧や履歴を表示しただけでは起動せず、スレッド作成、送信、名前変更などapp-server操作が必要になった時点で遅延起動する。デーモンごとに1プロセスを共有し、チャット数やプロジェクト数だけ増殖させない。

チャット入力または添付が存在する間は、選択モデルの提供元に対応するapp-serverを送信前に初期化し、通常のアイドル終了期限を更新する。Windowsクライアントはdaemon再接続中、app-server初期化中、モデル切替後の再初期化中は送信ボタンを無効にし、準備APIが成功した場合だけ有効にする。固定時間の経過だけでは送信可能と判定しない。

主に使用する操作は次のとおりである。

- 初期化: `initialize`、`initialized`
- スレッド: `thread/start`、`thread/name/set`、`thread/archive`
- ターン: `turn/start`、`turn/interrupt`、`turn/steer`
- 使用容量の取得

app-serverの通知はthread IDとturn IDでデーモン内のRunへ一意に対応付け、そのRunだけへ配送してWindowsクライアントへSSEで配信する。複数Runへ全通知を複製してから各監視処理で破棄する方式は使わない。Runは `queued`、`running`、`succeeded`、`failed`、`cancelled` の状態を持つ。app-serverモードでもRunをWSL側DBへ記録し、provider、thread ID、turn ID、最終通知、監視状態、終了理由を診断できるようにする。SSEイベントにはRun単位の連番を付け、Windowsクライアントで追跡が失われた場合は、デーモンの実行中Run一覧を正本として最後に受信した連番以後のイベントへ再接続する。イベントは短い間隔でまとめてWSL側DBにも保存し、再接続時にメモリ内履歴と重複しない形で再生する。保存イベントの単体サイズとRunごとの件数には上限を設け、途中経過の記録によってdaemonの応答性やDB容量を損なわないようにする。画面側のRun辞書はSSE接続とキャンセル処理の一時的な投影に限定し、送信可否やスピナーはデーモンとの定期照合で修正する。`thread/read` が `idle` を返して完了通知欠落を補う場合は、短い猶予時間を設けて受信済みの末尾差分と完了通知を先に処理する。ローカルDBの確定回答が途中で切れていて、同時刻のJSONL確定回答がその厳密な続きを含む場合は、履歴表示時にJSONLの全文へ回復する。

Windowsクライアントが選択中チャットの実行状態を照合するとき、daemonは起動済みの対象provider app-serverに限って `thread/read` を行う。送信開始からturn IDをRunへ登録するまでのチャットは開始中として予約し、状態照合から回復Runを作らない。threadが `active` なのに対応するRunも開始予約もない場合だけ、`thread/turns/list` を最新1件・item本文なしで取得し、`inProgress` のturnを回復Runとして登録して通知監視を再開する。停止中のapp-serverを状態確認だけのために起動せず、回復前に失われた出力断片からローカルの最終回答を合成せず、完了後に正規JSONL履歴を再読込する。

Windowsクライアント終了時に実行中Runがある場合は、そのチャットに再開フラグをWSL側DBへ記録してからdaemonとprovider app-serverを終了する。トップレベルdaemonをWindowsクライアント終了後に常駐させない。Windowsクライアントの次回起動時は再開フラグのある各チャットを同じ保存設定とproviderで再実行し、進行中turnそのものを再開する既知の方法がない場合は、そのチャットへ利用者指示「つづけて」を一度だけ自動送信する。Run開始に成功した時だけフラグを解除し、開始失敗時は次回起動用に保持する。チャット入力履歴は全会話履歴を再生成せず、daemonのローカルメッセージDBに保存済みの利用者指示からチャットごとに直近100件を専用APIで取得する。

Windowsクライアント起動直後はdaemonまたはprovider app-serverの準備中でも送信操作を有効にする。指示と添付は入力時のプロジェクト・チャットを伴うメモリ内送信キューへ即座に移し、入力欄を空にする。同じプロジェクト・チャットを表示中に送信準備が完了した時点で順番に送信し、別チャットの入力欄を上書きしたり別の宛先へ送信したりしない。

assistantメッセージのライブ表示では、`item/started`、`item/agentMessage/delta`、`item/completed`、`rawResponseItem/completed` の項目IDおよび `phase` を保持する。通常のitem通知ではphaseが未確定で、元Responses項目の完了通知で初めて確定する提供元については、その項目のテキストを確定までデーモン内で一時保持する。raw Responses項目とagent message項目のID体系が異なる場合は、完成した本文と保留中のdelta本文を照合して対応付ける。複数のraw Responses項目が1つのagent message項目へ集約されている場合は、raw項目の完成本文を保留中のdelta本文の先頭から順に消費し、raw項目ごとのIDとphaseで配信する。集約itemまたはturnの完了phaseで保留本文全体を結論へ昇格させない。`commentary` はitemごとに独立した「作業内容」、`final_answer` は「結論」として表示し、途中報告を相互または最終回答へ連結しない。同一item内で分割配信されるdeltaだけを連結する。phaseを最後まで特定できない複数itemをまとめて結論として保存するフォールバックは行わない。複数の `final_answer` 項目を受信した場合、最後の項目だけをチャット履歴の結論として保存する。既存JSONLの書き換えや過去表示の移行は行わない。

### 5.3 オートメーション

デーモン内のスケジューラーが有効なオートメーションを監視する。実行予定は、1分以上の分単位間隔、毎時の指定分（0〜59分）、毎日の指定時刻（ローカル時刻）から選択できる。Windowsクライアントでは方式と時・分をドロップダウンで選択させ、選択内容の具体例と動作説明を表示する。プロジェクトまたはチャットを切り替えたときは、前の選択に属するオートメーション一覧と編集欄を直ちにクリアし、新しい一覧の取得結果だけを表示する。選択中の登録または新規下書きを削除したときも、選択を解除して編集欄を直ちにクリアする。期限になると指定チャットへ保存済みプロンプトを送る。多重起動を防ぐ `running` 状態を持ち、次回時刻、最終実行時刻、最終エラーを記録する。手動の「今すぐ実行」も同じ実行サービスを使う。

オートメーション編集欄は、既存項目または「新規」で作成した下書きを選択するまで無効にする。

### 5.4 HTTP API

APIはWindowsクライアントおよび同一PC上のRemote Agent用のローカルAPIであり、外部公開しない。daemonは起動中のhost、port、PIDをWSL側の `~/.local/share/codex-lite/daemon-endpoint.json` に所有者だけが読める権限で記録し、正常終了時に自分のPIDと一致する場合だけ削除する。Remote Agentはこのファイルを使って既存daemonを発見し、見つからない場合だけ同梱起動スクリプトから遅延起動する。

| 分類 | 主なAPI |
|---|---|
| 稼働・設定 | `GET /health`、`GET /diagnostics`、`GET/PATCH /settings`、`POST /shutdown` |
| 使用状況 | `GET /usage/capacity`（OpenAI/Codexの5時間・1週間容量、Codexクレジット、DeepSeek選択時の残高） |
| プロジェクト | `GET/POST /projects`、`GET/PATCH/DELETE /projects/{id}`、`POST /projects/{id}/relocate`、履歴候補の一覧・取込 |
| チャット | 一覧、作成、取得、名前変更、アーカイブ、削除 |
| メッセージ | 一覧、カーソル付きページ取得、送信 |
| Run | 取得、保存済み結論1件の取得、SSEイベント、停止、追加指示 |
| ファイル | ディレクトリ一覧、テキスト内容取得、Markdown埋め込み用画像取得 |
| オートメーション | 一覧、作成、更新、即時実行、削除 |
| リモート操作 | `GET /remote/v1/catalog`、task ID付きの許可済み操作実行、Run SSEイベント。汎用APIとファイル取得は公開しない。画像添付はRemote Agent専用一時ディレクトリ内のファイルだけを許可する |

リモート操作ではクラウドtask IDをSQLiteの `remote_tasks` に記録する。完了済みtask IDの再要求には保存済み結果を返し、処理中または結果不明のtask IDは再実行しない。エラーは `error.code`、`error.message`、必要に応じて `error.details` を持つJSONで返す。

Remote Webの新規チャット画面にはWindowsクライアントと同じ使用状況を表示し、手動再取得を可能にする。OpenAI選択時は5時間容量・1週間容量・Codexクレジット、DeepSeek選択時は通貨ごとの残高を表示し、対象外の容量グラフは隠す。チャット入力欄では保存済み設定に基づくモデルと思考レベルを選択できる。実行中のRunは停止できる。ファイル選択と入力欄へのドラッグ＆ドロップにより、画像を含む任意のファイルを最大4件、合計600 KiBまで添付できる。Remote Agentは受信データを所有者専用の一時ファイルへ変換してapp-serverへ渡し、Run終了後に削除する。

Remote Agentはreasoningやツール進捗を件数と表示用本文へ集約し、開始元にかかわらず2秒以上の間隔を空けて対象チャット文書の`activeRun`へ反映する。カタログ同期は実行中の`activeRun`を簡易値で上書きせず、進捗監視だけを実行中投影の書き込み元とする。task文書を別の進捗正本にせず、Web開始Runのtaskは要求状態と承認イベントだけを保持する。表示用本文は直近24項目に限定し、項目ごとの文字数にも上限を設ける。本文を同じ集約更新へ含めることでFirestoreの書き込み回数は件数のみの場合から増やさず、検索しない進捗フィールドはインデックス対象外にする。実行中の進捗本文は上部の実行状態枠ではなく、デスクトップアプリと同様に会話履歴へ思考・作業項目として時系列表示する。ライブの思考・作業項目にもRemote Agentがイベントを観測した時刻を保持し、Webは永続履歴、送信中指示、ライブ進捗を同じ時刻順で描画する。永続履歴の後へライブ進捗を無条件に追加しない。各項目は時刻を履歴行の右端へ独立表示して折りたたみ可能にし、長い要約で時刻を画面外へ押し出さない。初期状態は閉じる。Runが完了してtaskやactiveRunが非アクティブになっても、同じrun IDのstatus項目が永続チャット履歴へ到着するまでは最後の進捗投影を表示し続け、到着後に永続履歴へ置き換える。結論の表示を理由に思考・作業項目を消さない。`agentMessage`等の境界通知とイベント詳細の生JSONは表示しない。最終回答など利用者に必要な出力は従来どおり転送する。

Remote用チャット文書は `users/{uid}/devices/{deviceId}/chats/{chatId}` に集約し、各文書の `projectId` で所属プロジェクトを表す。WebはPCごとにこのコレクションを1本だけリアルタイム購読し、プロジェクトごとのサブコレクション購読は行わない。会話履歴は件数や本文を切り詰めず、Firestoreの文書上限より十分小さい安定チャンクとして `users/{uid}/devices/{deviceId}/historyChunks` に保存する。履歴全体のJSONはUTF-8文字境界を壊さない断片へ分割し、各文書の文字列1フィールドへ保存してWebで連結後に配列へ復元する。単一の作業履歴が文書上限を超える場合も同じ断片形式で複数文書へ分割する。チャット文書には履歴改訂と件数だけを置き、Webは現在選択中のチャットに対応する履歴チャンクだけを追加でリアルタイム購読する。Remote Agentは履歴改訂が変わったチャットだけを取得し、ハッシュが変わったチャンクだけを書き込む。Run完了通知は末尾イベントのDB保存後に同期を起動し、開始元がWindowsかWebかによって確定履歴の経路を分けない。旧スキーマとの互換処理や複数の読み取り経路は設けない。

画像確認イベントの画像本体およびBase64 data URLはRemote用履歴へ含めず、Firestoreへ保存しない。画像のインライン表示は現段階ではローカルのデスクトップアプリだけで行う。

Remote Agentの端末状態更新、中断タスク照合、カタログ同期がFirestoreのクォータ超過または一時障害で失敗してもAgentプロセスを終了しない。起動時はWebタスクのリアルタイム購読とheartbeatを、大量になり得る履歴同期より先に開始する。ローカルカタログを取得した時点で実行中Runの監視も開始し、履歴移行やバックオフの完了をWeb送信・停止・進捗同期の前提にしない。デスクトップで既に進行中のRunへRemote Agentが接続するときは、カタログの`activeRunEventSequence`より後だけを購読し、過去のRunイベントを現在の進捗件数として再計上しない。WindowsクライアントがWeb起点Runを初めて発見したときはRun先頭から保存済みイベントを再生し、既に受信位置を保持している場合だけその位置より後へ再接続する。同じローカルカタログ版への再試行は待機時間を増やしながら最大3回に制限し、その後は新しいローカルカタログ版が発生するまで再試行しない。Firestore SDKが内部再試行している書き込みを外側のタイムアウトで放置して次の同期を重ねず、カタログ書き込みは常に1本だけ完了を待つ。heartbeatも前回書き込みが完了するまで重ねない。途中まで保存できた履歴チャンクはハッシュ照合で再書き込みせず、未保存または変更済みのチャンクだけを次回送る。

履歴チャンクは1文書ずつ個別にcommitし、実行状態や完了解除の書き込みが複数の大容量履歴文書からなるbatchの後ろで長時間待たないようにする。Firestore SDKの最大backoffまたは書き込み待ち行列超過は課金プランの利用上限と断定せず、書き込み混雑としてWindows画面へ表示する。

Webのオンライン状態インジケータはワークスペース全体ではなく、プロジェクトツリーの各PC行に表示する。

Webでプロジェクトだけを選択した新規チャット画面では、5時間・週間容量に加えて残りチャージ額を表示する。承認レベルはデスクトップと同じ「承認を求める」「自動で承認」「フルアクセス」から選び、モデル・思考レベルと同じRuntime設定経路で更新する。

デスクトップとWebからメッセージを送信した時は、デーモンまたはFirestoreへの引き渡し完了を待たずに入力欄と添付欄を空にし、チャット履歴の直下かつ入力欄の上にある送信キューへ即時表示する。送信キューは「送信待ち」「送信中」「送信エラー」を区別し、引き渡しが受理された項目をキューから除いて会話履歴へ表示する。複数の送信は受付順を保つ。

Webは最後に表示したPC・プロジェクト・チャットIDをFirebaseユーザー別にブラウザへ保存し、次回表示時にそのチャットが存在する場合は復元する。単に一覧先頭のチャットを初期選択しない。

Webでチャットを選択した時は `#/devices/{deviceId}/projects/{projectId}/chats/{chatId}` 形式のハッシュURLを設定する。このURLを直接表示した場合は、ブラウザ内の前回選択よりURLを優先して該当チャットを開く。

Webの会話本文は、生HTMLを実行せず、レベル1〜6の見出し、順序なし・順序付きの箇条書き、太字、斜体、インラインコード、コードブロック、HTTP(S)リンク、HTTP(S)インライン画像、表のMarkdown表示に対応する。

デスクトップアプリから開始したRunも同じローカルSSEをRemote Agentが監視し、選択中チャットの思考・作業件数と表示用本文をFirestoreへ同期する。画面下部は上からチャット履歴、進捗表示、チャット入力欄、操作行の順にする。操作行には承認レベル・モデル・思考、画像、送信を同じ行へ配置し、「追加指示」等の入力状態を言い換えるだけのラベルは表示しない。進捗枠には状態、件数、停止、承認だけを表示し、個々の思考・作業本文は会話履歴に表示する。Web開始Runとデスクトップ開始Runのどちらも停止できるようにする。WebからRunを開始した場合、daemonのカタログ変更通知を待つ単一の経路でWindowsクライアントが直ちに対象チャットとRunを取り込み、SSEの途中経過へ接続する。定期ポーリングはRun発見経路として使わない。

Runの開始・終了によるRemoteカタログ変更は、手動送信、Web送信、オートメーションを区別せず、Runイベントハブの`status`、`done`、`error`だけを通知元とする。HTTP操作完了後の追加通知や定期ポーリングをRun発見の代替経路にしない。これにより、ユーザーメッセージ保存より後にRunが登録される場合も、Remote Agentは登録済みRunを即時取得する。

Remote WebはFirestoreの`system/access`文書にある小文字の`entries`配列へ、ログイン中のメールアドレス全体またはそのドメインが一致し、Googleでメール確認済みの利用者だけを許可する。`entries`にはメールアドレスとドメインを区別せず列挙する。この文書はクライアントから直接読み書きできず、Firestore Rulesの判定だけに使う。

Remote WebのService Workerは同一オリジンのGETをネットワーク優先で取得し、画面遷移ではHTTPキャッシュを使わず最新HTMLを確認する。各WebビルドでWorkerへ一意なバージョンを埋め込み、更新されたWorkerを待機させず有効化して表示中のPWAを一度だけ再読み込みする。PWA起動時、オンライン復帰時、バックグラウンドから表示へ戻った時にもWorker更新を確認し、常時ポーリングは行わない。オフライン時は最後に取得したキャッシュを表示する。

Remote Agent起動時は、同じdevice配下で前プロセスが残した`claimed`、`running`、`waiting_for_approval`のtaskを`connection_lost`へ確定する。再起動後も古いtaskを実行中としてWebへ表示し続けない。まだAgentに取得されていない`queued` taskは対象外とし、通常どおり処理する。

Remote Webのモバイル表示ではプロジェクトツリーを画面外から開くサイドバーとする。メニューボタンで開き、背景タップまたはEscで閉じるほか、プロジェクトまたはチャットを選択した時点で自動的に閉じる。デスクトップ幅では従来どおり左ペーンへ常時表示する。

Remote Webのログイン後画面は外周を角丸カードにせず、ヘッダー下の利用可能領域全体をプロジェクトツリーと会話へ使用する。モバイルではソフトウェアキーボード表示中も入力欄と操作行をVisual Viewport内に残し、操作行は入力領域の下端へ固定して送信ボタンを画面外へ隠さない。

Remote Webのプロジェクトツリーでは、選択して表示していない間に履歴が更新されたチャットへ青い未読ドットを表示する。実行中または承認待ちのチャットにはスピナーを表示し、未読と重なる場合はスピナーを優先する。配下にインジケーター対象のチャットがあるプロジェクトを折りたたんでいる間は、隠れたチャット行の代わりにプロジェクト行へ同じ優先順位でインジケーターを表示し、展開中は重複表示しない。未読状態と最後に観測した履歴版はブラウザごとに保存し、チャットを表示した時点で既読にする。初めて同期した既存チャットは未読にしない。

モバイルではチャット履歴を含む画面内のどこからでも右へ64px以上スワイプした場合にサイドバーを開き、サイドバー表示中は左へ64px以上スワイプした場合に閉じる。横スワイプは画面全体のキャプチャ段階で検出する。縦方向へ48pxを超えて移動した操作は、通常のスクロールやページ操作として扱う。

Remote Webの入力欄はデスクトップ幅ではEnterで送信、Shift+Enterで改行する。モバイル幅（760px以下）ではEnterを改行として扱い、送信ボタンを押した場合だけ送信する。判定は現在の表示幅に追従する。

Remote Webの入力欄へクリップボード画像を貼り付けた場合は、画像ボタンで選択した場合と同じ添付一覧へ追加する。PNG、JPEG、GIF、WebPだけを受け入れ、画像を含まない通常のテキスト貼り付けは妨げない。

Remote Webの入力中テキストは、ログイン利用者、PC、プロジェクト、チャットの組み合わせごとにブラウザのlocalStorageへ下書きとして保存する。プロジェクトだけを選択した新規チャット用下書きは既存チャット用下書きと分離する。下書きはFirestoreへ送らず、最大50文脈を更新時刻順に保持し、送信した文脈だけ削除する。Firebase Hostingの更新でPWAが再読み込みされても、最後に表示していたプロジェクトまたはチャットと対応する下書きを復元する。

モバイルの画面高は固定の`100vh`ではなくVisual Viewportの`resize`へ追従し、ソフトウェアキーボード表示中も会話ペーンと入力欄を現在見えている範囲内へ収める。入力欄下端には端末のsafe areaを加算し、送信ボタンを画面下端の外へ配置しない。

## 6. Codexアプリのデータとの連携

### 6.1 読み取るデータ

Codex Liteは、選択中の本人の `CODEX_HOME` に対応する `sqlite` ディレクトリから `state*.sqlite` を探し、`threads` テーブルがあるDBを読み取り専用URIで開く。通常同期で読むのは次のthreadメタデータに限る。

- `id`: CodexスレッドID
- `cwd`: プロジェクトの作業ディレクトリ
- `title`、`preview`、`first_user_message`: 表示タイトルの決定材料
- `archived`、`archived_at`: アーカイブ状態
- `rollout_path`: 対応するJSONL履歴ファイル
- `created_at`、`updated_at`、`recency_at` と各ミリ秒版: 作成・更新時刻

DBは候補の存在、`threads` テーブル、必須カラムを検査してから読む。想定スキーマでない場合は推測で処理せず同期を停止し、理由だけを診断情報へ出す。認証情報、Cookie、トークン、APIキー、`auth.json` は読まない。

`cwd` はWindowsドライブ表記なら `/mnt/<ドライブ>/...` に変換し、実在するディレクトリだけを候補にする。`rollout_path` は実在する `.jsonl` で、選択中の `CODEX_HOME` の `sessions` または `archived_sessions` 配下にある場合だけ採用する。

対応するJSONLの `session_meta.payload.source.subagent.other` が `guardian` のスレッドは、Codexの自動承認判断に使われる内部セッションのため、プロジェクト候補とチャット一覧へ表示しない。その他のサブエージェントセッションは通常どおり同期する。

### 6.2 履歴の表示

チャット履歴はstate DBそのものから本文を読まず、検証済みのJSONLファイルを読み取って組み立てる。主に次を表示対象とする。

- ユーザーメッセージ
- assistantの作業中メッセージと最終回答
- 関数・ツール呼び出しの完了結果。`function_call` と `custom_tool_call` の両形式を扱い、コマンド実行は要約と詳細出力を分けて表示する
- Codexが公開したreasoning summary。非公開の推論内容や暗号化された推論データは表示しない

ライブ実行と履歴のツール表示では、取得できた実行メソッド、引数、コマンド、作業ディレクトリ、出力、終了状態を可能な限り省略せず表示する。連続する開始・終了イベントを単一の要約へ上書きせず、低レベルの出力差分は対応する実行項目の詳細へ追記する。認証情報、Cookie、token、API key、secretなどは表示前に伏せる。

Codex Lite自身が実行したRunの推論、コマンド、ツール等の途中経過は、Windows画面の一時状態を正本にせず、WSL側DBへ保存したRunイベントから安定したIDで履歴項目を再構築する。チャットを切り替えて戻った場合やクライアントを再起動した場合も同じ途中経過を表示する。対応する時間帯にJSONL由来の同種項目もある場合はRunイベント側を優先して二重表示を避け、保存イベントを持たない過去Runや外部から取り込んだチャットはJSONLから構築する。

reasoning要約、ツール、コマンドなどの折りたたみ可能な進捗行は、折りたたみ時にもヘッダー右端へ発生時刻を表示する。

壊れたJSON行や未対応イベントは読み飛ばす。JSONLのみから発見した履歴、アーカイブ済み履歴、現在の `CODEX_HOME/sessions` 外の履歴は表示専用とし、継続できない理由をUIへ返す。

### 6.3 同期と状態変更

Codex Liteの `chats` テーブルは、Codex threadの一覧表示に必要なローカル投影である。state DBからの同期は読み取り専用であり、Codex private DBへ直接書き込まない。

新規スレッド、メッセージ送信、追加指示、停止、名前変更、アーカイブなどの状態変更はapp-serverを使う。アーカイブ後はローカル投影も更新する。プロジェクト登録名などCodex Lite固有のUI状態はCodex Lite DBだけで管理する。

## 7. Codex Liteの保存データ

既定の保存先はWSL ext4上の `$HOME/.local/share/codex-lite` である。

```text
$HOME/.local/share/codex-lite/
  codex-lite.db
  settings.json
  runs/
  daemon-venv/
```

SQLiteには次を保存する。

- `projects`: 登録済みプロジェクトのID、表示名、パス、時刻
- `chats`: プロジェクトとの対応、タイトル、CodexスレッドID、JSONLパス、継続可否、アーカイブ状態
- `messages`: fake/互換実行用メッセージと種別
- `runs`: 実行状態、PID、終了コード、ログパス、エラー
- `automations`: 名前、プロンプト、実行予定の種類と値、有効状態、実行時刻、エラー

主要状態をWindows側 `.codex` や `/mnt/c` に保存しない。ただし、利用者が設定でWindows側 `.codex` をCodex HOMEとして選んだ場合は、Codex自身の既存状態を読み取る。

## 8. ファイルAPIの安全境界

ファイルAPIは読み取り専用である。相対パスを正規化して登録プロジェクトのroot配下であることを検証し、root外へのパストラバーサルやシンボリックリンク経由の逸脱を拒否する。ファイルサイズとテキスト判定の制限を設け、編集・作成・削除APIは持たない。

診断情報には実行環境、パスの存在、Codexバージョン、現在および直近のRun、Run監視の生存状態・最終通知・照合結果・終了理由、app-server状態などを含める。Windowsクライアントが保持するRunの投影、選択中chat ID、送信準備状態もdaemon側の状態と比較できるよう併記するが、入力内容、認証情報、秘密情報の内容は出力しない。

Windowsクライアントの `ui-performance.log` は10MBを上限とし、上限到達時は `ui-performance.previous.log` へ1世代だけローテーションする。起動時に既存ログが上限を超えている場合は末尾10MB以内だけを退避し、現在ログを空から開始する。同一イベントは1秒あたり20件までとし、2秒以内の同一内容を重複記録せず、抑制件数を要約して記録する。

## 9. 設定値

デーモンは既定値、`$HOME/.config/codex-lite/config.toml`、環境変数の順に設定を上書きする。主な環境変数は次のとおりである。

| 環境変数 | 内容 | 既定 |
|---|---|---|
| `CODEX_LITE_HOST` | bind先 | `127.0.0.1` |
| `CODEX_LITE_PORT` | ポート。0は自動選択 | `0` |
| `CODEX_LITE_WSL_DISTRO` | WSL名（デーモン単独起動時の診断用） | `WSL_DISTRO_NAME`、未設定時は空文字列 |
| `CODEX_LITE_APP_DATA_DIR` | データディレクトリ | `$HOME/.local/share/codex-lite` |
| `CODEX_LITE_DATABASE` | SQLiteパス | データディレクトリ内 |
| `CODEX_LITE_RUN_LOG_DIR` | Runログ | データディレクトリ内 `runs` |
| `CODEX_LITE_CODEX_HOME` | Codex HOME | `$HOME/.codex` |
| `CODEX_LITE_CODEX_SQLITE_HOME` | state DBディレクトリ | Codex HOME内 `sqlite` |
| `CODEX_LITE_CODEX_PATH` | Codex実行ファイル | PATHから解決 |
| `CODEX_LITE_MAX_CONCURRENT_RUNS` | 同時実行数 | `4` |
| `CODEX_LITE_ALLOW_MNT_C_PROJECTS` | `/mnt/c`を許可 | `true` |
| `CODEX_LITE_RUNNER` | `app-server` または `fake` | `app-server` |
| `CODEX_LITE_PERMISSION_PROFILE` | 初期アクセスポリシー | `:danger-full-access` |
| `CODEX_LITE_APPROVAL_POLICY` | 初期承認方法 | `never` |
| `CODEX_LITE_MODEL` | 初期モデル。空文字列はCodex既定 | 空文字列 |

Remote Webの青い未読インジケーターはチャット文書の最後に完了したRun IDが変わったときだけ付ける。履歴改訂、履歴項目数、初回同期、既存履歴の再移行は未読判定に使わない。初めて観測した完了Run IDは既読の基準値として保存し、新着扱いしない。

## 10. 現在の範囲外

- Codex Desktopの完全互換UI
- Codexの認証やログイン操作
- private Codex DBへの通常機能としての書き込み
- プロジェクトファイルの編集、作成、削除
- ブラウザ操作、Computer Use、各種外部サービス用UI
- Codex以外のAI実行エンジン
- 外部ネットワークへ公開するデーモン
