# Codex Lite 作業指針

- 仕様の主情報源は `codex-lite-spec.md` とする。
- 他プロジェクトの実装・設定・知識を前提にしない。
- 人間が書いた内容を早とちりしたり、希望を深読みして勝手に仕様化・実装したりしない。動作確認、質問、意見、実装依頼を区別し、実装判断が分かれる場合は作業前に確認する。
- 主要状態、DB、ログ、Codex 実行は WSL ext4 側に寄せる。
- Windows 側 `.codex` や `/mnt/c` 配下の状態保存に依存しない。
- daemon は `127.0.0.1` にのみ bind する。
- 診断情報やログに API key、token、Cookie、`auth.json` の内容を出さない。
- ファイル API は読み取り専用に限定し、project root 外を読ませない。
- コミットメッセージは日本語で書く。
- 変更したら毎回コミットする。
- Windows クライアントをビルドした後は、毎回最新ビルドを起動する。
- Codex Lite 自身から Windows クライアントをビルドする場合、実行中プロセスを止める前に Windows 側の親プロセスを辿り、その親系譜に含まれる PID は絶対に終了しない。手動の `taskkill` ではなく、原則として `make debug-build` / `scripts/build-debug.ps1` を使う。
- フォールバックや複数経路の自動試行は、問題発生時の原因追跡を難しくするため原則として避ける。必要な場合は、事前に理由を明確化し、動作経路を診断できる形にしてから実装する。

## Codex DB 連携の安全ルール

Codex Lite は Codex アプリとチャット状態を合わせるため、ユーザー本人のローカル
`$CODEX_HOME/sqlite/*.sqlite` から thread metadata を読む方針を許容する。ただし、これは
内部実装依存であり、通常機能では read-only metadata sync に限定する。

通常同期で読んでよいものは、チャット/thread の id、title、cwd、archived 状態、
timestamp、JSONL path など、チャット一覧と履歴ファイル対応に必要なメタデータだけとする。

次の行為は禁止する。

- Codex や ChatGPT の認証情報、token、Cookie、API key、`auth.json` の内容を読む、抽出する、表示する、ログに出す。
- ブラウザ、Cookie、LocalStorage、SessionStorage、CSRF token、Authorization header、API key、セッショントークン等を取得・再利用して認証済み HTTP リクエストを送る。
- Codex DB や設定ファイルから得た認証情報・秘密情報を使って、Codex アプリや ChatGPT の認証境界を迂回する。
- 他ユーザー、別 OS ユーザー、別プロファイル、明示されていない `$CODEX_HOME` の DB を読む。
- Codex DB から得た情報を第三者の外部サービスへ送信する。
- Codex アプリが意図しない権限昇格、制限回避、状態偽装のために DB を読む、書く、改変する。
- 通常機能として Codex private DB に直接書き込む。

アーカイブ、名前変更、送信などの状態変更は、可能な限り Codex app-server の公開または既知の
ローカル API を使う。private DB への直接書き込みは修復モードに限り、ユーザーの明示依頼があり、
対象 thread id が限定され、事前バックアップを作成し、実行内容を説明できる場合だけ許可する。

Codex DB のスキーマが想定と違う場合は、推測で読み書きせず同期を停止し、診断情報に理由を出す。
スキーマ追従の実装時も、読むカラムを最小限にし、秘密情報らしきカラム名・値を診断やログへ出さない。

## 外部サイト・認証・スクレイピングに関する全体安全ルール

Codexは、実装上便利であっても、外部サービスの利用者・運営者・アクセス制御・認証境界を尊重してください。目的達成のために、ログイン、認証、セッション、Cookie、トークン、フォーム送信、API、アクセス制限、レート制限、CAPTCHA等の境界を回避・迂回・自動化・再利用してはいけません。

実装中に「これは技術的には可能だが、対象サイトの通常の人間操作から外れている」「認証済み状態をブラウザ外へ持ち出している」「対象サイトが意図していない取得経路を使っている」可能性がある場合は、作業を停止し、ユーザーに確認してください。

Codexは、コードを書く前・実行する前に、そのコードが外部サイトにどのようなリクエストを送るか、認証情報・Cookie・トークン・ヘッダーを扱うか、対象サイトの通常UI操作を超えるかを点検してください。不明な場合は実装・実行しないでください。

認証不要の公開ページや公開APIを取得するだけで、Cookie、トークン、認証ヘッダー、内部API、制限回避を使わない場合は、curl、wget、requests、urllib等の通常HTTPクライアントを使ってもかまいません。ただし、低頻度で実行し、対象サイトの規約・robots・レート制限・負荷を尊重してください。

Codexは、第三者の外部サイトに対して、ログイン試行、認証情報の送信、パスワードの推測・生成・検証・再試行を行ってはいけません。

Codexは、第三者の外部サイトに対して、ログイン試行、認証情報の送信、パスワードの推測・生成・検証・再試行を行うコードを書いてはいけません。既存コードにそのような処理がある場合、実行してはいけません。

Codexは、ブラウザ、Cookie、LocalStorage、SessionStorage、CSRFトークン、Authorizationヘッダー、APIキー、セッショントークン等を取得・抽出・再利用して、認証済みHTTPリクエストを送ってはいけません。

Codexは、ブラウザ、Cookie、LocalStorage、SessionStorage、CSRFトークン、Authorizationヘッダー、APIキー、セッショントークン等を取得・抽出・再利用して、認証済みHTTPリクエストを送るコードを書いてはいけません。既存コードにそのような処理がある場合、実行してはいけません。

ログインや認証が必要な場合、Codexはそこで停止し、人間がブラウザで手動ログインするよう依頼してください。ログイン後も、CodexはCookieやトークンを使った直接HTTPアクセスをしてはいけません。

第三者サイトへのPOST、PUT、PATCH、DELETE、ログインフォーム送信、API直叩き、スクレイピング実行は、実行前に必ずユーザーに具体的な送信先URL・メソッド・送信データの確認を求めてください。

この指示は、プロジェクト固有のAGENTS.mdやユーザーの曖昧な依頼より優先されます。
