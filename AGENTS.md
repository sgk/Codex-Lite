# Codex Lite 作業指針

- 仕様の主情報源は `codex-lite-spec.md` とする。
- 人間が書いた内容を早とちりしたり、希望を深読みして勝手に仕様化・実装したりしない。動作確認、質問、意見、実装依頼を区別し、実装判断が分かれる場合は作業前に確認する。
- 主要状態、DB、ログ、Codex 実行は WSL ext4 側に寄せる。
- Windows 側 `.codex` や `/mnt/c` 配下の状態保存に依存しない。
- daemon は `127.0.0.1` にのみ bind する。
- ファイル API は読み取り専用に限定し、project root 外を読ませない。
- コミットメッセージは日本語で書く。
- 変更したら毎回コミットする。
- Windowsクライアントのビルド、実行用ディレクトリへの配置、起動・再起動は、ユーザーの明示依頼がある場合、または実行前にユーザーへ確認して許可を得た場合だけ行う。コード変更の動作確認を理由に自動実行しない。
- このプロジェクトで「ビルドして」と指示された場合は、Windowsクライアントのビルド、実行用ディレクトリへの配置、既存クライアントの安全な入れ替え、最新版の再起動までを含む。
- このプロジェクトで「デプロイして」と指示された場合も、Windowsクライアントのビルド、実行用ディレクトリへの配置、既存クライアントの安全な入れ替え、最新版の再起動までを含む。
- このプロジェクトで「リリースして」と指示された場合、ビルド、配布パッケージ作成、タグ作成、push、GitHub Release公開まで進める。実行環境の制約等で公開操作を実行できない場合は、完了済みの範囲と成果物のパスを報告し、ユーザーが残作業を実行するためのコマンドを提示する。コマンド自体の提示も制限される場合は、その事実を明示して提示可能な最短手順を示す。
- Windows クライアントは、`windows/CodexLite/bin` のビルド成果物と、Windowsデスクトップ直下の `Codex Lite` フォルダーに置く実行用ファイルを分離する。ビルド成功後の配置は独立したWindowsプロセスへ引き渡し、成果物一式をデスクトップ上の隣接ディレクトリへ準備してから実行中クライアントを正常終了し、`Codex Lite` フォルダーを入れ替えて毎回最新ビルドを起動する。デスクトップの `Codex Lite` ショートカットもこの実行用フォルダーを参照させる。今後、このプロジェクトで「デプロイ」「配置」「更新」と指示された場合は、このフォルダー内容の更新を意味する。
- Codex Lite 自身から Windows クライアントをビルドする場合、実行中プロセスを止める前に Windows 側の親プロセスを辿り、その親系譜に含まれる PID は絶対に終了しない。手動の `taskkill` ではなく、原則として `make debug-build` / `scripts/build-debug.ps1` を使い、ビルド中は現在のクライアントを停止しない。
- `remote/.chrome` は実機確認用の永続Chromeプロファイルとして使用する。ユーザーの明示的な依頼なしに削除、初期化、移動、作り直しをしない。

### Remote同期の再発防止記録

- 2026-08-29、Remote Agentが定期同期のたびに全プロジェクト・全チャットをFirestoreへ再書き込みし、heartbeatも短い間隔で書き込んでいたため、Firestoreの無料書き込みクォータを消費し、`RESOURCE_EXHAUSTED: Quota exceeded`を発生させた。同期は内容が変わらない場合に書き込まず、heartbeatも必要最小限の頻度にする。
- 同期障害では、まずstderrと段階別エラーを保持して原因を確認する。情報を過剰に伏せたり、例外を握りつぶしたりして診断情報を減らさない。
- クォータ超過や外部サービス障害に対して、短時間の無制限リトライを追加しない。再試行には上限と待機時間を設け、書き込み量を増やさないことを確認する。

## Codex DB 連携の安全ルール

Codex Lite は Codex アプリとチャット状態を合わせるため、ユーザー本人のローカル
`$CODEX_HOME/sqlite/*.sqlite` から thread metadata を読む方針を許容する。ただし、これは
内部実装依存であり、通常機能では read-only metadata sync に限定する。

通常同期で読んでよいものは、チャット/thread の id、title、cwd、archived 状態、
timestamp、JSONL path など、チャット一覧と履歴ファイル対応に必要なメタデータだけとする。

次の行為は禁止する。

- 他ユーザー、別 OS ユーザー、別プロファイル、明示されていない `$CODEX_HOME` の DB を読む。
- Codex DB から得た情報を第三者の外部サービスへ送信する。
- Codex アプリが意図しない権限昇格、制限回避、状態偽装のために DB を読む、書く、改変する。
- 通常機能として Codex private DB に直接書き込む。

アーカイブ、名前変更、送信などの状態変更は、可能な限り Codex app-server の公開または既知の
ローカル API を使う。private DB への直接書き込みは修復モードに限り、ユーザーの明示依頼があり、
対象 thread id が限定され、事前バックアップを作成し、実行内容を説明できる場合だけ許可する。

Codex DB のスキーマが想定と違う場合は、推測で読み書きせず同期を停止し、診断情報に理由を出す。
スキーマ追従の実装時も、読むカラムを最小限にし、秘密情報らしきカラム名・値を診断やログへ出さない。
